<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Vue formulaire projet avec contrôles GED -->
    <record id="project_project_form_ged_controls" model="ir.ui.view">
        <field name="name">project.project.form.ged.controls</field>
        <field name="model">project.project</field>
        <field name="inherit_id" ref="project.edit_project"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet" position="inside">
                <!-- Onglet Contrôles GED -->
                <notebook>
                    <page string="🎛️ Contrôles GED" name="ged_controls">
                        <group>
                            <group string="📋 Configuration GED">
                                <field name="x_department"/>
                                <field name="x_cliq_channel" placeholder="Canal Cliq (ex: #projet-innovation)"/>
                                <field name="x_workdrive_folder" placeholder="Dossier WorkDrive (ex: /Innovation/2025)"/>
                            </group>
                        </group>
                        
                        <div class="alert alert-info" role="alert">
                            <strong>💡 Gestion des accès:</strong><br/>
                            Les permissions GED se configurent dans <b>Paramètres → Utilisateurs → Utilisateurs</b><br/>
                            Section <b>"GED Contrôles"</b> pour chaque utilisateur :<br/>
                            • GED User : Accès lecture seule<br/>
                            • GED Manager : Gestion complète<br/>
                            • GED Innovation/Expansion/Gouvernance : Accès par département
                        </div>
                    </page>
                </notebook>
            </xpath>
        </field>
    </record>

    <!-- Vue liste avec colonnes GED -->
    <record id="project_project_list_ged" model="ir.ui.view">
        <field name="name">project.project.list.ged</field>
        <field name="model">project.project</field>
        <field name="inherit_id" ref="project.view_project"/>
        <field name="arch" type="xml">
            <xpath expr="//list" position="inside">
                <field name="x_department"/>
                <field name="x_cliq_channel"/>
                <field name="x_workdrive_folder"/>
            </xpath>
        </field>
    </record>

</odoo>